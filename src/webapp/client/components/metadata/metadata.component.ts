import { Component } from '@angular/core';

@Component({
    selector: 'metadata',
    template: require('./metadata.html'),
    styles: [require('./metadata.scss')]
})
export class MetadataComponent {

    Router;
}
